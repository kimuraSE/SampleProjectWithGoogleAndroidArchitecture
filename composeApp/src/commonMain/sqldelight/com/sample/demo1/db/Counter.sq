CREATE TABLE CounterEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    value INTEGER NOT NULL
);

selectValue:
SELECT value FROM CounterEntity WHERE id = 1;

upsertValue:
INSERT OR REPLACE INTO CounterEntity(id, value) VALUES (1, :value);